# Dipendenze per Elysium Pozioni
# Tkinter è incluso nella maggior parte delle installazioni Python.
# Se non presente, installarlo con:
#   sudo apt install python3-tk   (Linux)
#   brew install python-tk        (macOS)

tk

# Icone dei materiali nella GUI (gui_main.py: from PIL import Image, ImageTk)
pillow>=10.0

# Solo per generare l'eseguibile: pyinstaller ElysiumPozioni.spec
# pyinstaller>=6.0
